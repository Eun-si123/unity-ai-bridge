{
  "name": "unity-ai-bridge-workspace",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=24.19.0 <25"
  },
  "scripts": {
    "build": "npm --prefix mcp-server run build",
    "test": "npm --prefix mcp-server test"
  }
}
